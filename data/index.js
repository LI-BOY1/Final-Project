const trainerData = require('./trainers');
const courseData = require('./courses');
const commentData = require('./comments');
const memberData = require('./members');

module.exports = {
    trainers: trainerData,
    courses: courseData,
    comments:commentData,
    members:memberData
};